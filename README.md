# 0314_retro_mobile_dash

모바일/PC 모두 동작하는 레트로 회피 게임.

## 플레이
- 모바일: 화면 버튼(◀ / ▶ / START)
- PC: `←` `→` 또는 `A` `D`, 시작은 `Space`

## 룰
- 떨어지는 블록을 피하면서 점수를 올립니다.
- 충돌 시 라이프 감소, 라이프 0이면 종료.
- 최고 점수는 브라우저 로컬 저장소에 저장됩니다.

## 실행
정적 사이트이므로 `index.html`을 브라우저로 열면 바로 실행됩니다.
